<h1 class="mb-4">📚 Cartes mémorisation - {{moduleName}}</h1>

<div class="row">
  <div class="col-md-8">
    <!-- Zone des cartes -->
    <div id="flashcard-container" class="card">
      <div class="card-body">
        <div id="flashcard-content" style="min-height: 300px; display: flex; align-items: center; justify-content: center; flex-direction: column;">
          <p class="text-muted">Chargement des cartes...</p>
        </div>
      </div>
      <div class="card-footer text-center">
        <button class="btn btn-outline-primary" id="prev-btn">← Précédente</button>
        <span id="card-counter" class="mx-3">0 / 0</span>
        <button class="btn btn-outline-primary" id="next-btn">Suivante →</button>
      </div>
    </div>

    <div class="mt-3">
      <a href="/entrainement.html" class="btn btn-secondary">← Retour à l'entraînement</a>
    </div>
  </div>

  <!-- Barre latérale -->
  <div class="col-md-4">
    <div class="card bg-light">
      <div class="card-body">
        <h5 class="card-title">📊 Progression</h5>
        <div class="progress mb-3">
          <div id="progress-bar" class="progress-bar" style="width: 0%"></div>
        </div>
        <p id="progress-text" class="mb-0">0 / 0 cartes</p>
      </div>
    </div>

    <div class="card mt-3">
      <div class="card-body">
        <h5 class="card-title">⚙️ Options</h5>
        <div class="form-check">
          <input class="form-check-input" type="checkbox" id="shuffle-mode" />
          <label class="form-check-label" for="shuffle-mode">Mélanger les cartes</label>
        </div>
        <div class="form-check">
          <input class="form-check-input" type="checkbox" id="flip-auto" />
          <label class="form-check-label" for="flip-auto">Retournement automatique</label>
        </div>
      </div>
    </div>
  </div>
</div>

<script src="/js/flashcards.js"></script>
