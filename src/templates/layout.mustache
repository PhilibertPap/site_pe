<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - Préparation PE</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <!-- Navigation persistante -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold d-flex align-items-center gap-2" href="index.html">
                <img src="assets/annales/qcm/2022/image1.png" alt="Logo SUF Marine" class="suf-navbar-logo" loading="lazy">
                <span>Patron d'Embarcation</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link{{#isIndex}} active{{/isIndex}}" href="index.html" {{#isIndex}}aria-current="page"{{/isIndex}}>Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link{{#isParcours}} active{{/isParcours}}" href="parcours.html" {{#isParcours}}aria-current="page"{{/isParcours}}>Parcours</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link{{#isEntrainement}} active{{/isEntrainement}}" href="entrainement.html" {{#isEntrainement}}aria-current="page"{{/isEntrainement}}>Entraînement</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link{{#isExamens}} active{{/isExamens}}" href="examens.html" {{#isExamens}}aria-current="page"{{/isExamens}}>Examens</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link{{#isCarnet}} active{{/isCarnet}}" href="carnet.html" {{#isCarnet}}aria-current="page"{{/isCarnet}}>Carnet</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Contenu principal -->
    <main class="container py-5">
        {{{content}}}
    </main>

    <!-- Pied de page -->
    <footer class="site-footer text-white py-4 mt-5">
        <div class="container text-center">
            <p class="mb-0">Préparation au Patron d'Embarcation - Groupe NDC</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="js/qcm-engine.js"></script>
    <script src="js/app.js"></script>
</body>
</html>
