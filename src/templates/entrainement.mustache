<h1 class="mb-4">🎯 Espace d'entraînement</h1>

<div class="row mb-4">
  <div class="col-md-8">
    <p class="lead">Préparez-vous efficacement avec nos <strong>{{statistics.totalQCM}}</strong> QCM gratuits</p>
  </div>
</div>

<!-- Statistiques -->
<div class="row mb-4">
  <div class="col-md-3">
    <div class="card bg-light">
      <div class="card-body text-center">
        <h5>Total QCM</h5>
        <h3 class="text-primary">{{statistics.totalQCM}}</h3>
      </div>
    </div>
  </div>
  <div class="col-md-3">
    <div class="card bg-light">
      <div class="card-body text-center">
        <h5>Catégories</h5>
        <h3 class="text-success">{{statistics.themes}}</h3>
      </div>
    </div>
  </div>
  <div class="col-md-3">
    <div class="card bg-light">
      <div class="card-body text-center">
        <h5>Types de tests</h5>
        <h3 class="text-info">{{statistics.typeOfTests}}</h3>
      </div>
    </div>
  </div>
  <div class="col-md-3">
    <div class="card bg-light">
      <div class="card-body text-center">
        <h5>Prix</h5>
        <h3 class="text-warning">{{statistics.price}}</h3>
      </div>
    </div>
  </div>
</div>

<!-- Sessions d'entraînement -->
<div class="row mb-5">
  {{#trainingSessions}}
  <div class="col-md-6 mb-3">
    <div class="card h-100 cursor-pointer training-card" data-session="{{id}}" style="cursor: pointer;">
      <div class="card-body">
        <div class="text-4xl mb-2">{{icon}}</div>
        <h5 class="card-title">{{name}}</h5>
        <p class="card-text">{{description}}</p>
        <small class="text-muted">{{type}}</small>
      </div>
    </div>
  </div>
  {{/trainingSessions}}
</div>

<!-- Détails de la session sélectionnée -->
<div id="session-details" class="mb-5"></div>

<!-- Avis utiles -->
<div class="alert alert-info alert-dismissible fade show">
  <h4 class="alert-heading">💡 Conseils</h4>
  <p>Si vous débutez, commencez par les <strong>tests thématiques</strong> pour un apprentissage progressif.</p>
</div>

<script src="/js/training.js"></script>
